[package]
name = "hal"
version = "0.1.0"
edition = "2024"

[lib]
test = false
bench = false

[features]
default = ["board-qemu-virt"]

board-qemu-virt = ["driver-pl011", "driver-fw-cfg", "driver-ramfb"]
board-rk3126 = []

driver-pl011 = []
driver-fw-cfg = []
driver-ramfb = ["driver-fw-cfg"]


[dependencies]
